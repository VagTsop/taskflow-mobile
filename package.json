{
  "name": "taskflow-mobile",
  "private": true,
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run dev --prefix packages/backend\" \"npm run dev --prefix packages/mobile\"",
    "dev:backend": "npm run dev --prefix packages/backend",
    "dev:mobile": "npm run dev --prefix packages/mobile"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
